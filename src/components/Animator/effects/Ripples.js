class Ripple {
  constructor(analyser, canvas, ctx, params) {
    this.analyser = analyser;
    this.canvas = canvas;
    this.ctx = ctx;
    this.params = params;
  }

  draw() {
    console.log('drawing...');
  }
}

export default Ripple;
